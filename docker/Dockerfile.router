FROM golang:1.26-alpine AS build

WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o /router ./cmd/router

FROM gcr.io/distroless/static-debian12:nonroot
COPY --from=build /router /router
EXPOSE 8081
ENTRYPOINT ["/router"]
